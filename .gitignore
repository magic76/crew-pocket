# Dependencies
node_modules/
package-lock.json

# Temporary Uploads & Scratch
uploads/
scratch/

# System & OS
.DS_Store
*.log

# Android helper build artifacts and local signing keys
android/crew-helper/bin/
android/crew-helper/*.keystore
android-wrapper/.gradle/
android-wrapper/**/build/
android-wrapper/local.properties
android-wrapper/*.keystore
# Optional locally-vendored native Codex runtime. Never commit binaries.
android-wrapper/app/src/main/jniLibs/arm64-v8a/libcodex_exec.so
android-wrapper/app/src/main/jniLibs/arm64-v8a/libc++_shared.so
# Optional locally-vendored embedded Node runtime and patched dependencies.
android-wrapper/app/src/main/jniLibs/arm64-v8a/libnode_exec.so
android-wrapper/app/src/main/jniLibs/arm64-v8a/libcrew_node_*.so

# Local custom HTML tools, available at /extra/ but never committed.
public/extra/
*.apk
